name: redo
services:
    redo:
        image: ghcr.io/dinkelk/redo:latest
        build:
            context: ..
            dockerfile: docker/Dockerfile
        container_name: redo_container
        volumes:
            - type: bind
              source: ../../redo
              target: /workspace/redo
        command: sleep infinity
